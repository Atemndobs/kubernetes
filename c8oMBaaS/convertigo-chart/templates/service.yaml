apiVersion: v1
kind: Service
metadata:
  name: {{ include "convertigo-chart.fullname" . }}
  labels:
    {{- include "convertigo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "convertigo-chart.selectorLabels" . | nindent 4 }}
